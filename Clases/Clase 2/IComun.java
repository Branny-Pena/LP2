//similar a la herencia pero solo para comportamientos comunes, no para atributos
//estos metodos se tienen que implementar en las clases que esten definidos en la
//interfaz

interface IComun{
	void mostrarCodigoPUCP();
}